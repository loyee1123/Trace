---
name: worklog-2026-07-29
description: 2026-07-29 工作日志：权重下载、demo 客户端、部署踩坑四连与 conda-forge 解法
metadata:
  type: project
---

2026-07-29 工作记录（目标：OpenCUA-7B demo——集群起服务 + 笔记本控制键鼠）：

**完成**
1. OpenCUA-7B 权重下载完毕（16G，28 分片；教训：`huggingface-cli` 已弃用且静默失败，必须用 `hf download`）
2. 通读官方仓库，向用户讲清用法：vLLM 服务→OpenAI API 调用→OSWorld 版 OpenCUAAgent 多步循环（3 图历史 + 反思 CoT + qwen25 坐标）；研究阶段可复用 AgentNetTool→DataProcessor→CoTGenerator 数据管线
3. 写好笔记本端试用客户端 `opencua/demo_client.py`：截屏→模型→smart-resize 坐标换算（含 Retina 适配）→逐步确认→pyautogui 执行；已推送
4. 部署踩坑四连（详见 [[cluster-gpu-cuda-constraint]]）：
   - 老 libstdc++ 缺 CXXABI_1.3.15 → slurm 脚本 module purge + LD_LIBRARY_PATH 修复 ✓
   - OpenCUA 自定义 tokenizer 与 transformers 5.x 不兼容 → 补丁 HF 缓存 ✓
   - vLLM 0.26 的 OpenCUA 集成 bug（transformers 5 按 __init__ 签名推断 processor 属性，image_processor 丢失）→ 三行补丁 ✓
   - **驱动只到 CUDA 12.9 + glibc 2.28 → PyPI vllm 全灭** → 改用 conda-forge vllm 0.19.1 cuda129 构建（transformers 会回到 4.x，上面两个 5.x 补丁自然失效不再需要）

**当前状态**
- trace 环境：pip 版 vllm/torch/transformers 栈已卸载，conda-forge `vllm=0.19.1=cuda129_py311*` 安装中
- 装好后**暂不提交 GPU 任务**（用户明确指示，等批准）
- 下一步：用户批准后 sbatch → 报节点名 → 用户笔记本建隧道跑 demo_client.py
