---
name: worklog-2026-08-29
description: 2026-08-29 工作日志：Claude Code 方案 A 实测成功；开源模型选型定 Qwen3.8-27B 并在现有 trace 环境跑通服务
metadata:
  type: project
---

2026-08-29 工作记录：

**上午：方案 A（Claude Code 控制桌面）实测**
- 装 Claude Code 踩坑：官方 `irm install.ps1` 在国内网络下先 ECONNREFUSED、后被劫持返回网页 HTML；学校 VPN 是分流的不带 Anthropic 流量；最终**npm 路线（npmmirror 镜像）装成功**，登录订阅账号即用（[[user-profile]] 已记网络约束）
- 用户反馈：**"Claude 现在任务完成得很好"** —— 方案 A 验证通过，Claude 作为 GUI agent 效果明显优于 OpenCUA-7B

**下午：本地化替代 —— 开源模型选型与部署**
- 调研结论见 [[open-cua-models-2026-08]]：4×ica100（A100 80GB）可跑 Qwen3.8-27B（84.3%）/ Holo3.1-35B-A3B 等；用户拍板 **Qwen3.8-27B，且不换环境**
- 关键验证：Qwen3.8 config 架构名为 `Qwen3_5ForConditionalGeneration`，**vllm 0.19.1 注册表已含**；config 新字段（output_gate_type / attn_output_gate / mtp_use_dedicated_embeddings）连 vllm 0.27.1 与 transformers main 都未使用，attn_output_gate 0.19.1 已支持，mtp.* 权重 0.19.1 会跳过 → **无需升级环境**（conda-forge 已有 0.27.1 cuda129 py311/312 构建备用）
- Qwen 的 OSWorld 分数用的 harness 是公开的：`xlang-ai/OSWorld/mm_agents/qwen/`（QwenAgent，OpenAI 兼容接口，`computer_use` 函数定义 + XML `<tool_call>` 输出 + 1000×1000 相对坐标 + smart_resize(factor=32, max_pixels=16·16·4·12800) + 旧截图折叠）→ 已移植为 `qwen/qwen_client.py`（Windows 版，含 DPI/稳健点击/中文剪贴板/防死循环，`--think`/`--confirm` 开关）
- 权重下载 56GB：第一次 47GB 处被登录节点 OOM 杀（exit 137）→ `hf download --max-workers 4` 断点续传成功；残留 `.incomplete` 需手动清
- **服务跑通**：`qwen/serve_qwen38.slurm`（ica100 2 卡 TP=2，`--reasoning-parser qwen3`，`--max-num-batched-tokens 20480`）在 icgpu02 启动成功——权重 25.7GB/卡、加载 2 分钟、torch.compile 81 秒、总计约 8 分钟就绪；API 冒烟通过。fla/ops 的 UserWarning 是正常预热提示
- 用户笔记本 E 盘临时找不到（`E:\Trace` 不存在），未能实测客户端；**用户要求终止任务，改天再测**（空跑 17 分钟，教训见 [[confirm-before-heavy-actions]]）

**下次继续**
1. 用户到位后再 `sbatch qwen/serve_qwen38.slurm`（需用户当场允许）
2. 笔记本 `git pull` → 隧道到新节点 → `python qwen_client.py "..."`，对比 Claude 的表现；再试 `--think`
