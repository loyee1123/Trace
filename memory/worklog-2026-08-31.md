---
name: worklog-2026-08-31
description: 2026-08-31 工作日志：Qwen3.8 客户端首次实测 + 演示学习(record_demo + --demo)落地
metadata:
  type: project
---

2026-08-31 工作记录：

**服务与实测**
- 起服务两次：第一次分到 icgpu04 报 "CUDA unknown error"（**节点抽风，非配置问题**，`sbatch --exclude=icgpu04` 解决）；icgpu05 正常，torch.compile 有缓存后启动更快。用完已按用户要求 scancel
- 笔记本坑两个：PowerShell 里 `python` 是商店占位符（静默无输出），**用 cmd 或 `py`**；SSH 隧道中途断开表现为客户端 `APIConnectionError [WinError 10061]`——服务端没事，重连隧道重跑即可
- qwen_client 首次实测：浏览器任务前两步定位/执行正常；换到专业医疗软件（选前列腺癌测试病人、下载 CT nii.gz）出现**同一行反复点击死循环**。两轮修复已推送：①提示词加 Windows 习惯（桌面图标/列表行需 double_click、win键开应用）②防重复反馈（重复时注入"换法"提示+温度0→0.6）——实测反馈生效（模型从单击改双击）但仍卡在该行。**未决问题：点击到底有没有作用到该应用**（若应用以管理员权限运行，模拟点击会被 Windows UIPI 拦截→需管理员身份开 cmd）；待用户确认"行有没有高亮"。开 `--think` 可提升陌生界面定位

**演示学习（Trace 核心命题第 1 层）落地**
- `qwen/record_demo.py`：pynput 录制器——记录点击（双击自动合并）/文本输入/快捷键/滚动，后台 0.5s 缓存截图使每步存的是"动作前"画面，F10 结束，存 `demos/<名>/demo.jsonl + step_*.png`
- `qwen_client.py --demo demos/a,demos/b`：单/多演示注入提示词（坐标转 1000×1000 相对网格；多演示时明示"共同部分=固定流程，差异=灵活处"）
- 层次路线已与用户对齐：①上下文演示（已实现）→②演示自动总结成 SOP 库→③攒轨迹 LoRA 微调
- **微调路径已设计好**（等数据量到位）：demo.jsonl 每步展开为（系统提示+任务+历史+截图→Action+tool_call XML）样本，格式与运行时 harness 一致；4×A100 80GB 上 LoRA 微调 Qwen3.8-27B（LLaMA-Factory/ms-swift，需确认 Qwen3.8 架构支持）；vLLM `--enable-lora` 部署零改客户端。数据量预期：几条 demo 用上下文模式即可，二三十任务×2-5 遍才值得 LoRA；实验梯度 zero-shot vs 上下文演示 vs LoRA 即科研故事主干
- **demos/ 目录 = 数据集**，用户日常用 `--demo` 干活时录的每条都要留存

**遗留**
- 医疗软件点击是否被拦截（管理员权限问题）待确认
- 用户需求画像更新：想要"演示一次，之后带思考地重复执行"——正中项目主线
