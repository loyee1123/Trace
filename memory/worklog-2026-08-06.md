---
name: worklog-2026-08-06
description: 2026-08-06 工作日志：demo 实测迭代（防死循环/终止判定），Claude 作为核心模型的两套方案落地
metadata:
  type: project
---

2026-08-06 工作记录：

**上午：OpenCUA demo 实测迭代**（接 [[worklog-2026-08-04]] 的端到端跑通）
- 用户实测反馈驱动的三轮修复，全部推送：默认连续执行（--confirm 保留逐步模式，Ctrl+C/鼠标甩左上角退出）→ 防死循环（动作重复时提示模型换法 + temperature 0→0.6 + 连续 4 次熔断）→ 终止判定（系统提示显式约定 terminate(status=...)，解析器兼容 computer.* 动作空间）
- 测完 scancel 释放 GPU。结论：7B 模型可用但费劲——定位、终止判断、防重复都要客户端兜底

**下午：核心模型换 Claude 的探索与落地**
- 用户问能否把 core model 换成 Claude → 关键区分：**订阅（Pro/Max）≠ API 额度**，订阅覆盖 Claude.ai/Claude Code，Messages API 要单独充值
- 落地两套方案到仓库 `claude/` 目录（066ca02）：
  - **方案 A（订阅额度，零费用）**：Claude Code 当 agent 大脑。`tools/screenshot.py`（截屏缩放到长边 1366 省 token + scale 写 meta json）+ `tools/act.py`（click/type/key/scroll/drag，DPI 适配 + robust click + 中文输入走剪贴板 clip/utf-16-le）+ `CLAUDE.md` 工作循环手册（动作后必须截屏验证、重复两次必须换法、登录/付款弹窗停下问用户）
  - **方案 C（API 计费）**：`computer_client.py` 用官方 computer use 工具。要点：工具版本 `computer_20251124`、beta 头 `computer-use-2025-11-24`、模型 claude-opus-5；截图以 base64 image 放 tool_result 回传；历史只留最近 3 张截图控 token；坐标按 display_width_px 声明的缩放视图换算回物理屏
- 架构含义：换 Claude 后集群/GPU/vLLM 都不需要，笔记本直连。代价是不可微调——**AgentNet 微调路线仍要 OpenCUA**，习惯学习若走记忆/提示词注入路线则 Claude 也可做（两条线并存，见 [[fara-vs-opencua]]）

**待验证**：用户尚未实测方案 A/C（国内网络到 Anthropic 可能要代理）
